# 仓库管理系统核心流程梳理

## 一、入库操作完整流程

### 1. 前端流程 (Inbound.vue)

#### 页面初始化
- 组件挂载时 (`onMounted`) 执行三个操作：
  1. `fetchMaterials()`: 调用 GET `/api/materials` 获取所有物资列表
  2. `fetchSuppliers()`: 调用 GET `/api/suppliers` 获取所有供应商列表
  3. `generateSerialNo()`: 生成入库单号（格式：IN-YYYYMMDDHHMMSS）

#### 表单交互
- 物资选择变化时 (`handleMaterialChange`):
  - 根据选中的物资ID，自动填充该物资的默认单价
  - 如果该物资关联了默认供应商，自动选中该供应商

#### 前端校验
- 提交前检查是否选择了物资（`material.id` 不能为空）
- 若未选择供应商，则将 `supplier` 设为 `null`

#### 提交操作
- 调用 POST `/api/inventory/inbound` 提交入库数据
- 成功后重置表单并重新生成新的入库单号

---

### 2. 后端流程

#### Controller 层 (InventoryController.java:19-22)
```java
@PostMapping("/inbound")
public ApiResponse<InboundRecord> inbound(@RequestBody InboundRecord record) {
    return ApiResponse.success(inventoryService.processInbound(record));
}
```
- 接收 `InboundRecord` JSON 实体
- 直接委托给 `InventoryService.processInbound()` 处理

#### Service 层 (InventoryService.java:23-36)
**核心方法：`processInbound()`**

| 步骤 | 操作 | 代码位置 |
|------|------|----------|
| 1 | 设置入库时间为当前时间 | InventoryService.java:25 |
| 2 | 根据物资ID查询物资实体，不存在则抛出异常 | InventoryService.java:28-29 |
| 3 | **更新库存数量**：物资库存 += 入库数量 | InventoryService.java:31 |
| 4 | 保存更新后的物资实体 | InventoryService.java:32 |
| 5 | 关联物资实体到入库记录（确保关系正确） | InventoryService.java:34 |
| 6 | 保存入库记录到数据库 | InventoryService.java:35 |

**事务特性**：使用 `@Transactional` 注解，整个流程在一个事务中执行，保证库存更新和入库记录保存的原子性。

#### 数据落库
- `InboundRecord` 表字段：id, serialNo, inboundTime, quantity, price, material_id, supplier_id, remark
- `Material` 表更新：stockQuantity 字段增加

---

## 二、出库操作及库存不足处理

### 1. 前端流程 (Outbound.vue)

#### 库存预校验
- 选择物资后 (`handleMaterialChange`)：显示该物资的当前库存
- 数量输入框的最大值限制为当前库存数量 (`:max="currentStock > 0 ? currentStock : 1"`)
- **提交前校验** (Outbound.vue:44-47)：
  ```javascript
  if (form.value.quantity > currentStock.value) {
    ElMessage.error(`库存不足，当前库存: ${currentStock.value}`);
    return;
  }
  ```

#### 提交操作
- 调用 POST `/api/inventory/outbound` 提交出库数据
- 成功后重置表单、刷新库存数据

---

### 2. 后端库存不足处理逻辑 (InventoryService.java:38-55)

**核心方法：`processOutbound()`**

| 步骤 | 操作 | 代码位置 |
|------|------|----------|
| 1 | 设置出库时间为当前时间 | InventoryService.java:40 |
| 2 | 根据物资ID查询物资实体，不存在则抛出异常 | InventoryService.java:43-44 |
| 3 | **库存校验**：如果当前库存 < 出库数量 | InventoryService.java:46 |
| 4 | 校验失败：抛出 `RuntimeException`，消息包含当前库存 | InventoryService.java:47 |
| 5 | 校验通过：物资库存 -= 出库数量 | InventoryService.java:50 |
| 6 | 保存更新后的物资实体 | InventoryService.java:51 |
| 7 | 关联物资实体并保存出库记录 | InventoryService.java:53-54 |

**库存不足时的处理**：
- 后端通过 `throw new RuntimeException("Insufficient stock! Current: " + material.getStockQuantity())` 抛出异常
- 异常由 `GlobalExceptionHandler` 统一处理（需确认，但Spring Boot默认会返回500错误）
- 前端收到错误响应时，由 axios 拦截器或调用处处理（当前代码未显式处理后端校验失败的情况）

**双重校验机制**：
1. 前端预校验：提升用户体验，减少无效请求
2. 后端校验：保证数据一致性，防止并发或绕过前端的情况

---

## 三、ECharts 统计图数据聚合逻辑

### 1. 数据源
图表数据全部来源于 **GET `/api/materials`** 接口返回的物资列表数据，后端不做专门的聚合处理，所有聚合都在**前端完成**。

---

### 2. Dashboard.vue 图表聚合

#### 统计卡片数据 (computed: stats)
- **仓库总数**：`warehouses.value.length`
- **物资种类**：`materials.value.length`
- **库存预警**：`materials.value.filter(item => item.stockQuantity < item.alertThreshold).length`
- **库存总值**：`materials.value.reduce((sum, item) => sum + (item.stockQuantity * item.price), 0)`

#### 饼图：库存分类分布 (Dashboard.vue:56-65)
```javascript
const categoryData: Record<string, number> = {};
materials.value.forEach((item: any) => {
  const catName = item.category?.name || '未分类';
  categoryData[catName] = (categoryData[catName] || 0) + item.stockQuantity;
});
```
**聚合逻辑**：
1. 初始化空对象 `categoryData` 用于存储分类统计
2. 遍历所有物资，按分类名称分组：
   - 有分类的使用 `category.name`
   - 无分类的归类到 "未分类"
   - 相同分类的库存数量累加
3. 最终转换为 ECharts 需要的 `[{name, value}]` 格式

#### 折线图：近7日出入库趋势
- **注意**：目前使用的是**硬编码的演示数据** (Dashboard.vue:89-97)
- 数据：入库 [120, 132, 101, 134, 90, 230, 210]，出库 [220, 182, 191, 234, 290, 330, 310]
- 尚未对接真实的历史出入库记录API

---

### 3. StockReport.vue 图表聚合

#### 饼图：库存分类分布 (StockReport.vue:31-40)
- **逻辑与 Dashboard 完全相同**：按分类名称聚合库存数量

#### 柱状图：库存价值 Top 5 (StockReport.vue:43-46)
```javascript
const sortedByValue = [...materials.value].map((item: any) => ({
  name: item.name,
  value: item.stockQuantity * item.price
})).sort((a, b) => b.value - a.value).slice(0, 5);
```
**聚合逻辑**：
1. 计算每个物资的库存价值 = 库存数量 × 单价
2. 按价值从高到低排序
3. 取前5条作为图表数据

#### 其他统计数据
- 物资总数：列表长度
- 库存总量：所有物资 `stockQuantity` 求和
- 库存总价值：所有物资 `(stockQuantity × price)` 求和
- 预警物资：`stockQuantity < alertThreshold` 的物资数量

---

## 四、整体架构总结

```
前端表单提交
    ↓
[前端校验] 必填项、库存预检查
    ↓
HTTP POST /api/inventory/inbound (或 outbound)
    ↓
[InventoryController] 接收请求
    ↓
[InventoryService] @Transactional
    ├─ 查询物资实体
    ├─ 业务校验（出库检查库存）
    ├─ 更新物资库存数量
    └─ 保存出入库记录
    ↓
数据库落库（materials 表 + inbound_records/outbound_records 表）

图表数据流程：
GET /api/materials → 前端数组遍历聚合 → ECharts 渲染
```

---

## 五、关键代码位置索引

| 功能 | 文件路径 | 行号 |
|------|---------|------|
| 入库前端表单 | `frontend/src/views/inventory/Inbound.vue` | 38-61 |
| 出库前端表单及预校验 | `frontend/src/views/inventory/Outbound.vue` | 39-64 |
| 入库后端处理 | `backend/src/main/java/com/warehouse/service/InventoryService.java` | 23-36 |
| 出库后端处理及库存校验 | `backend/src/main/java/com/warehouse/service/InventoryService.java` | 38-55 |
| Dashboard图表聚合 | `frontend/src/views/Dashboard.vue` | 39-98 |
| 库存报表图表聚合 | `frontend/src/views/report/StockReport.vue` | 22-88 |
| 入库实体定义 | `backend/src/main/java/com/warehouse/entity/InboundRecord.java` | 1-35 |
| 出库实体定义 | `backend/src/main/java/com/warehouse/entity/OutboundRecord.java` | 1-33 |
| 物资实体（含库存字段） | `backend/src/main/java/com/warehouse/entity/Material.java` | 1-36 |
