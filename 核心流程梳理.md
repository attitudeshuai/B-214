# 仓库管理系统核心流程梳理

## 一、项目技术栈概览

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + TypeScript + Element Plus + ECharts |
| 后端 | Spring Boot + Spring Data JPA + Lombok |
| 数据库 | MySQL (通过 Docker 部署) |
| 通信 | RESTful API，统一 `ApiResponse` 包装 |

---

## 二、入库流程（前端提交 → 数据库落库）

### 2.1 整体调用链路

```
用户填写表单 (Inbound.vue)
  → 前端校验
  → axios.post('/inventory/inbound', payload)
  → Axios 拦截器 (axios.ts)
  → InventoryController.inbound() 
  → InventoryService.processInbound()  [@Transactional]
  → materialRepository.findById() + material.setStockQuantity()
  → materialRepository.save() + inboundRecordRepository.save()
  → 数据库落库
```

### 2.2 前端阶段 (Inbound.vue)

1. **页面初始化**：`onMounted` 时调用 `fetchMaterials()` 加载物资列表、`fetchSuppliers()` 加载供应商列表、`generateSerialNo()` 自动生成入库单号（格式 `IN-yyyyMMddHHmmss`）
2. **选择物资联动**：当用户在 `<el-select>` 中选择物资时，触发 `handleMaterialChange`，自动填充该物资的默认单价和默认供应商
3. **表单字段**：
   - `serialNo`：入库单号（自动生成，可手动刷新）
   - `material.id`：关联的物资 ID（必选）
   - `quantity`：入库数量（最小值 1）
   - `price`：入库单价（最小值 0，精度 2 位小数）
   - `supplier.id`：供应商 ID（可选，为空时置为 null）
   - `remark`：备注（可选）
4. **前端校验**：仅检查 `material.id` 是否已选择，未选择则 `ElMessage.error('请选择物资')` 并阻止提交
5. **提交**：构造 payload，若 `supplier.id` 为空则将 supplier 置 null，发起 `POST /inventory/inbound`
6. **响应处理**：若 `res.code === 200` 则提示"入库成功"并重置表单、重新生成单号

### 2.3 网络层 (axios.ts)

- `baseURL` 设为 `/api`，实际请求路径为 `/api/inventory/inbound`
- 响应拦截器直接返回 `response.data`（即后端 `ApiResponse` 的 JSON 体）
- 错误拦截器提取 `error.response.data.message` 显示 `ElMessage.error`

### 2.4 后端 Controller 层 (InventoryController.java:19-21)

```java
@PostMapping("/inbound")
public ApiResponse<InboundRecord> inbound(@RequestBody InboundRecord record) {
    return ApiResponse.success(inventoryService.processInbound(record));
}
```

- 接收 `@RequestBody` 自动反序列化为 `InboundRecord` 实体
- 没有额外的参数校验注解（如 `@Valid`），直接透传给 Service

### 2.5 后端 Service 层 (InventoryService.java:23-36)

`processInbound` 方法标注 `@Transactional`，保证以下操作在同一事务中：

1. **设置入库时间**：`record.setInboundTime(LocalDateTime.now())`
2. **查找物资**：`materialRepository.findById(record.getMaterial().getId())`，找不到则抛出 `RuntimeException("Material not found")`
3. **增加库存**：`material.setStockQuantity(material.getStockQuantity() + record.getQuantity())`
4. **保存物资**：`materialRepository.save(material)` —— 更新 `materials` 表的 `stock_quantity`
5. **设置关联**：`record.setMaterial(material)` 确保关联关系
6. **保存入库记录**：`inboundRecordRepository.save(record)` —— 写入 `inbound_records` 表

### 2.6 数据库层

涉及两张表：
- **materials**：`stock_quantity` 字段被更新（累加入库数量）
- **inbound_records**：插入一条新记录，包含 `serial_no`、`inbound_time`、`quantity`、`price`、`material_id`、`supplier_id`、`remark`

### 2.7 异常处理

- 物资不存在时：`RuntimeException("Material not found")` → 被 `GlobalExceptionHandler.handleRuntimeException()` 捕获 → 返回 `{ code: 400, message: "Material not found", data: null }`
- 数据库约束违反（如 `serial_no` 重复）：被 `handleException()` 捕获 → 返回 `{ code: 500, message: "...", data: null }`
- 前端 Axios 错误拦截器会提取 `message` 字段弹窗提示

---

## 三、出库流程及库存不足处理

### 3.1 整体调用链路

```
用户填写表单 (Outbound.vue)
  → 前端校验（含库存不足预检）
  → axios.post('/inventory/outbound', payload)
  → InventoryController.outbound()
  → InventoryService.processOutbound()  [@Transactional]
  → 库存不足校验 → 抛异常或扣减库存
  → materialRepository.save() + outboundRecordRepository.save()
  → 数据库落库
```

### 3.2 前端阶段 (Outbound.vue)

1. **页面初始化**：加载物资列表，自动生成出库单号（格式 `OUT-yyyyMMddHHmmss`）
2. **选择物资联动**：`handleMaterialChange` 将当前物资的 `stockQuantity` 赋值给 `currentStock` 变量
3. **表单字段**：
   - `serialNo`：出库单号
   - `material.id`：物资 ID（必选）
   - `quantity`：出库数量（最小 1，最大值为 `currentStock`，若库存为 0 则上限为 1）
   - `department`：领用部门
   - `remark`：备注
4. **前端库存不足校验**（第 44-47 行）：
   ```javascript
   if (form.value.quantity > currentStock.value) {
     ElMessage.error(`库存不足，当前库存: ${currentStock.value}`);
     return;
   }
   ```
   这是在前端进行的一次预检，直接拦截请求不发往后端
5. **物资下拉框**：显示格式为 `物资编号 - 物资名称 (库存: X)`，让用户直观看到当前库存
6. **提交成功后**：重新调用 `fetchMaterials()` 刷新物资列表以更新库存数据

### 3.3 后端 Service 层 (InventoryService.java:38-55)

`processOutbound` 方法标注 `@Transactional`：

1. **设置出库时间**：`record.setOutboundTime(LocalDateTime.now())`
2. **查找物资**：`materialRepository.findById()`，找不到抛 `RuntimeException`
3. **库存不足校验**（第 46-48 行）：
   ```java
   if (material.getStockQuantity() < record.getQuantity()) {
       throw new RuntimeException("Insufficient stock! Current: " + material.getStockQuantity());
   }
   ```
   这是后端的核心校验，如果出库数量大于当前库存，直接抛出异常，**整个事务回滚**，不会扣减库存也不会保存出库记录
4. **扣减库存**：`material.setStockQuantity(material.getStockQuantity() - record.getQuantity())`
5. **保存物资**：`materialRepository.save(material)`
6. **保存出库记录**：`outboundRecordRepository.save(record)`

### 3.4 库存不足时的完整处理流程

库存不足时存在**双重校验机制**：

| 校验层级 | 位置 | 行为 |
|----------|------|------|
| 前端预检 | `Outbound.vue` 第 44 行 | 拦截请求，弹窗提示"库存不足，当前库存: X"，不发送 HTTP 请求 |
| 后端校验 | `InventoryService.java` 第 46 行 | 抛出 `RuntimeException`，事务回滚，返回 400 错误，前端弹窗显示后端错误信息 |

后端校验是最终的安全保障，即使前端校验被绕过（如直接调用 API），后端仍会拒绝超量出库。事务的 `@Transactional` 注解保证：库存扣减和出库记录写入要么同时成功，要么同时回滚，不会出现扣了库存但没记录的情况。

### 3.5 出库记录查看 (InventoryRecords.vue)

- 通过 `GET /inventory/records/outbound` 获取全部出库记录
- 表格展示：出库单号、物资名称、数量、出库时间、领用部门、备注

---

## 四、ECharts 统计图数据聚合逻辑

系统中有两处使用 ECharts 的页面，数据来源和聚合方式相同。

### 4.1 数据来源

两个页面的图表数据都来源于同一个 API：

```
GET /api/materials → MaterialService.getAllMaterials() → materialRepository.findAll()
```

返回的是 `materials` 表的全部记录，每条记录包含 `stockQuantity`（当前库存）、`price`（单价）、`category`（分类对象，含 `name`）、`alertThreshold`（预警阈值）。

**没有专门的后端聚合接口**，所有数据聚合都在前端完成。

### 4.2 StockReport.vue 的图表聚合

#### 饼图：库存分类分布

```javascript
const categoryData: Record<string, number> = {};
materials.value.forEach((item) => {
    const catName = item.category?.name || '未分类';
    categoryData[catName] = (categoryData[catName] || 0) + item.stockQuantity;
});
```

聚合逻辑：
1. 遍历所有物资
2. 以物资的分类名称（`category.name`）为 key，若分类为空则归入"未分类"
3. 对同一分类下所有物资的 `stockQuantity` 求和
4. 最终转换为 `[{ name: 分类名, value: 该分类库存总量 }]` 格式传给 ECharts

#### 柱状图：库存价值 Top 5

```javascript
const sortedByValue = [...materials.value].map((item) => ({
    name: item.name,
    value: item.stockQuantity * item.price
})).sort((a, b) => b.value - a.value).slice(0, 5);
```

聚合逻辑：
1. 对每个物资计算 `库存价值 = stockQuantity × price`
2. 按库存价值降序排列
3. 取前 5 名
4. 分别将 `name` 和 `value` 数组传给 ECharts 的 x 轴和 series.data

### 4.3 Dashboard.vue 的图表聚合

#### 饼图：库存分布

聚合方式与 StockReport.vue 的饼图完全相同：按分类名称分组，累加 `stockQuantity`。

#### 折线图：近7日出入库趋势

```javascript
xAxis: { data: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'] },
series: [
    { name: '入库', data: [120, 132, 101, 134, 90, 230, 210] },
    { name: '出库', data: [220, 182, 191, 234, 290, 330, 310] }
]
```

这是**硬编码的演示数据**，未对接任何后端接口。代码注释也明确标注了 `Mock Trend Data (Since we don't have historical data API yet)`。

### 4.4 统计卡片的聚合

| 页面 | 卡片 | 聚合方式 |
|------|------|----------|
| StockReport | 物资总数 | `materials.length` |
| StockReport | 库存总量 | `materials.reduce(sum + stockQuantity)` |
| StockReport | 库存总价值 | `materials.reduce(sum + stockQuantity × price)` |
| StockReport | 预警物资数 | `materials.filter(stockQuantity < alertThreshold).length` |
| Dashboard | 仓库总数 | `warehouses.length`（来自 `GET /warehouses`） |
| Dashboard | 物资种类 | `materials.length` |
| Dashboard | 库存预警 | `materials.filter(stockQuantity < alertThreshold).length` |
| Dashboard | 库存总值 | `materials.reduce(sum + stockQuantity × price)` |

### 4.5 数据聚合方式总结

```
后端 (materials 全量数据)
  → GET /api/materials
  → 前端接收 materials 数组
  → JavaScript 计算:
      - 按分类分组求和 → 饼图
      - 每项 stockQuantity × price 排序取 Top 5 → 柱状图
      - 全量求和/筛选 → 统计卡片
```

所有图表的聚合计算都在前端 JavaScript 中完成，后端只负责返回 `materials` 表的原始数据，没有做任何 SQL 层面的聚合（如 GROUP BY）。

---

## 五、数据库表关系

```
categories  1──N  materials  N──1  suppliers
                    │
                    ├── 1──N inbound_records (也关联 suppliers)
                    └── 1──N outbound_records (含 department 字段)
```

- `materials.stock_quantity` 是库存的**唯一权威值**，入库时 +N，出库时 -N
- `inbound_records` 和 `outbound_records` 是流水记录表，只增不改，用于历史追溯
- 库存预警基于 `materials.stock_quantity < materials.alert_threshold` 的比较
